# SubTrack security remediation plan

Plan date: 2026-09-17

## Purpose

This plan converts the findings in `SECURITY-AUDIT.md` into reviewable work and records completed remediation. Phase 4 is complete for local fictional data. Phase 5 may proceed only under `PHASE-5-SECURITY-GATE.md`; production deployment and real statements remain blocked.

## Completed remediations

| Finding | Completed control                                                                                                                       | Verification                                                                                                | Remaining work                                                                            |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| SEC-001 | Added baseline CSP and supporting browser headers and removed `X-Powered-By`.                                                           | Header assertions, the full browser suite, the production build, and a direct production-header probe pass. | Approve a strict nonce or hash strategy and HSTS at the production deployment boundary.   |
| SEC-002 | Replaced complete subscription objects at Client Component boundaries with explicit field-allowlisted DTOs and server-only DAL queries. | DTO unit tests and serialized-response browser checks pass.                                                 | Continue exact payload allowlists for every new client boundary.                          |
| SEC-003 | Added HTTPS-only URL validation, stored-value revalidation, visible destination hostnames, and explicit opener isolation.               | Ten URL unit cases and the provider-link browser test pass.                                                 | Reuse the schema at future write boundaries and approve shared-guide provenance controls. |
| SEC-004 | Implemented request-scoped SSR Auth, verified claims, confirmation, recovery, bounded sessions, HttpOnly cookies, and logout.           | Registration, recovery, cookie, protected-route, and logout browser tests pass.                             | Verify hosted provider settings and add recent-authentication controls in Phase 6.        |
| SEC-005 | Implemented explicit grants, RLS, owner-safe foreign keys, and two-user isolation. Future tables remain closed.                         | Forty-nine pgTAP assertions and a known-ID browser isolation test pass.                                     | Add mutation policies only with each owning phase.                                        |
| SEC-006 | Implemented dynamic private routes, refresh-header propagation, and private no-store production responses.                              | Dedicated `next start` cache test passes.                                                                   | Repeat against hosted CDN and reverse proxy before deployment.                            |
| SEC-011 | Scanned the available repository history and working checkpoint with Gitleaks.                                                          | No leak found.                                                                                              | Repeat in CI and after credential changes.                                                |

Priority meanings:

- **P0:** Blocks the next implementation phase or requires immediate containment.
- **P1:** Required within the named feature phase before sensitive behavior becomes available.
- **P2:** Required before public deployment.
- **P3:** Defense in depth or future integration work.

## Immediate blockers

No exposed secret, reachable Critical vulnerability, or reachable High dependency vulnerability requires emergency containment in the current fictional Phase 1 application.

The following design work is nevertheless an immediate **no-go gate** for Phase 2:

| Finding | Exact task                                                                                                                                                                                                               | Priority | Dependencies                                       | Effort | Verification method                                                                                                           | Blocking phase |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------- | -------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------- | -------------- |
| SEC-004 | Approve the Supabase SSR authentication and session lifecycle, including verified server identity, cookie attributes, refresh, expiry, revocation, password reset, generic errors, abuse controls, and reauthentication. | P0       | Supabase project architecture and deployment model | Medium | Design review plus executable test cases for forged, expired, revoked, logged-out, reset, and replayed sessions               | Before Phase 2 |
| SEC-005 | Approve deny-by-default database grants, per-operation RLS, ownership derivation, object authorization, and the table-by-table verification matrix in `THREAT-MODEL.md`.                                                 | P0       | Final Phase 2 schema ownership model               | Medium | RLS matrix review with named owner, anonymous, other-user, ownership-change, and denied-write tests                           | Before Phase 2 |
| SEC-006 | Approve protected rendering and caching rules for authenticated HTML, RSC payloads, session refresh responses, and logout.                                                                                               | P0       | Authentication design and hosting/CDN assumptions  | Small  | Header and cache test plan proving that two users cannot receive each other's response or cookie                              | Before Phase 2 |
| SEC-002 | Approve a `server-only` DAL and route-specific minimal DTO pattern. Identify the allowed fields for every Phase 2 client boundary.                                                                                       | P0       | Initial query and UI data requirements             | Medium | Type review and RSC payload assertions showing that database rows and unused private fields do not cross to Client Components | Before Phase 2 |
| SEC-009 | Approve the sensitive-data inventory, purpose, retention, support access, backup expiry, export, and deletion behavior for every Phase 2 data type.                                                                      | P0       | Product and privacy decisions                      | Medium | Signed data-inventory review and deletion/export acceptance criteria                                                          | Before Phase 2 |

## Required before Phase 2

| Finding | Exact task                                                                                                                                                                                                                                          | Priority | Dependencies                                                           | Effort | Verification method                                                                      | Blocking phase                                 |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- | ---------------------------------------------- |
| SEC-007 | Define a structured security-event allowlist, safe field schema, redaction policy, retention, access roles, alert thresholds, integrity requirements, and logger-failure behavior.                                                                  | P0       | Authentication and database event inventory                            | Medium | Review event catalog; unit-test plan proves prohibited values never enter logs           | Before Phase 2 design approval                 |
| SEC-008 | Approve the statement-import contract in `THREAT-MODEL.md`, including numeric limits, parser isolation, temporary storage, cleanup, deduplication, formula-neutralized export, and redacted audit events. Do not implement the pipeline in Phase 2. | P0       | Hosting resource model and product import limits                       | Medium | Security design review confirms every receive-to-delete stage has a control and test     | Before Phase 2; implementation blocks Phase 4  |
| SEC-001 | Review the implemented Phase 1 header baseline and approve the future strict CSP and HTTPS-only HSTS deployment decision. Do not add HSTS before confirming the production host boundary.                                                           | P1       | Hosting model, Supabase endpoints, and nonce/static-rendering decision | Medium | Policy review, browser/header assertions, and CSP violation review                       | Strict policy before private public deployment |
| SEC-011 | Repeat secret and tracked-file review in the canonical Git repository with history available.                                                                                                                                                       | P0       | Access to canonical clone and approved history scanner                 | Small  | Clean history-aware secret scan, tracked-ignored-file review, and branch-policy evidence | Before Phase 2 credentials are introduced      |

## Required while implementing authentication and Supabase

| Finding | Exact task                                                                                                                                                                                                                       | Priority | Dependencies                                                 | Effort | Verification method                                                                                                   | Blocking phase                                                         |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------ | ------ | --------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| SEC-004 | Implement request-scoped Supabase server clients and Next.js 16 session refresh behavior. Verify identity on the server for every protected read and write. Keep tokens out of web storage, props, logs, and user-facing errors. | P1       | Approved session design                                      | Large  | Integration tests for all session states and direct invocation of every server entry point                            | Phase 2 completion                                                     |
| SEC-005 | Implement least-privilege grants and per-operation RLS in reviewed migrations. Derive `user_id` from `auth.uid()`, use `using` and `with check`, and reject ownership reassignment.                                              | P1       | Approved schema and RLS matrix                               | Large  | pgTAP matrix plus two-user server integration tests for every table and operation                                     | Phase 2 completion                                                     |
| SEC-002 | Implement the `server-only` DAL, explicit authorization checks, narrow SQL selection, and route-specific DTOs. Never pass service credentials or database row types into Client Components.                                      | P1       | Authentication and schema implementation                     | Large  | Static import-boundary checks, DTO tests, bundle scan, and browser payload inspection                                 | Phase 2 completion                                                     |
| SEC-006 | Make private routes request-bound, prevent shared caching, preserve Supabase refresh cache headers, and exclude user data from static generation and shared cache keys.                                                          | P1       | Session refresh implementation and deployment cache          | Medium | Two-user tests through production-like cache; response-header and build-artifact inspection                           | Phase 2 completion                                                     |
| SEC-007 | Implement one structured security-event interface for authentication, authorization failure, export, deletion, and configuration changes. Use opaque IDs and safe codes only.                                                    | P1       | Approved event catalog and audit table                       | Medium | Unit and integration tests plus captured-log scans for tokens, cookies, emails, financial values, and CR/LF injection | Phase 2 completion                                                     |
| SEC-009 | Implement approved account export, deletion sequencing, backup expiry documentation, and least-privilege support access. Store only fields with an approved purpose.                                                             | P1       | Data inventory, schema, backup design, and account lifecycle | Large  | Export completeness tests; primary-store and backup-restoration deletion exercises; access review                     | Phase 2 completion where applicable; remaining controls before Phase 6 |

## Completed Phase 3 controls

- Subscription ownership comes only from verified claims.
- Strict form schemas reject unknown, duplicate, malformed, and protected fields.
- Money converts to integer minor units before persistence.
- Column grants exclude ownership, provenance, confidence, identifiers, and audit timestamps.
- Owner RLS policies independently enforce insert, update, and delete.
- Optimistic concurrency rejects stale edits, archives, and deletes.
- URL and date relationship constraints protect direct Data API writes.
- Archive, local delete, cancelled status, and provider cancellation remain distinct.
- Mutation audit events contain no financial details.

## Required before accepting real statement files

The application implements the controls below for bounded local fictional CSV files. Real statements remain blocked until the deployment-specific verification in the final column passes.

| Finding | Exact task                                                                                                                                                                                      | Priority | Dependencies                                | Effort | Verification method                                                                                         | Blocking phase                                                 |
| ------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- |
| SEC-008 | Implement the authenticated receive, validate, parse, normalize, detect, review, approve, persist, delete, and redacted-audit pipeline with the approved numeric limits.                        | P1       | Completed locally                           | Done   | Unit, hostile-fixture, database, and end-to-end approval tests pass                                         | Verify production ingress and resource behavior                |
| SEC-008 | Enforce one active import per user, account and network rate limits, bounded inputs, idempotency, user-scoped cryptographic fingerprints, and transactional duplicate prevention.               | P1       | Completed locally                           | Done   | Account-limit, concurrency, retry, collision, and cross-user tests pass                                     | Verify trusted proxy headers and edge limits                   |
| SEC-008 | Keep raw bytes in bounded request memory only, never persist them, and clear retained buffers on every success and failure path.                                                                | P1       | Completed locally                           | Done   | Code-path review, storage-schema assertions, malformed input, and oversized-body tests pass                 | Verify production memory limits and monitoring                 |
| SEC-008 | Treat headers and cells as untrusted text, normalize Unicode, reject prohibited controls and unsupported encodings, bound dimensions, and neutralize formula-capable values only at CSV export. | P1       | Completed locally                           | Done   | Fixtures for malformed CSV, duplicate headers, nulls, controls, oversized fields, and formula prefixes pass | Add fuzzing before broad format support                        |
| SEC-007 | Add import lifecycle audit events and generic safe error mappings without filenames, rows, merchant text, amounts, encodings, parser stacks, or paths.                                          | P1       | Completed locally                           | Done   | Exact audit-detail allowlist and prohibited-content assertions pass                                         | Configure hosted retention, access, integrity, and alerting    |
| SEC-003 | Apply HTTPS-only server validation to user-editable websites and cancellation URLs, reject credentials and controls, and show the destination host before navigation.                           | P1       | Shared URL schema and cancellation workflow | Small  | Unit tests for unsafe protocols and browser tests for displayed host and safe new-tab behavior              | Phase 3 for editable websites; Phase 6 for cancellation guides |

## Completed Phase 5 controls

- Price candidates use only newest owner-linked transactions in the subscription currency.
- PostgreSQL re-derives price changes, checks noise and ratio bounds, locks current versions, and appends immutable history.
- Category overlap is advisory, currency-separated, excludes inactive and uncategorized services, and uses a bounded owner preference.
- Budget, potential savings, and realized savings use integer minor units.
- Realized savings require explicit provider confirmation and clear on reactivation.
- Calendar dates use date-only windows, while reminder timestamps apply the profile IANA time zone.
- Reminder event identity prevents duplication and resurrection after read or dismissal.
- In-app notification delivery has no email, SMS, or push transport.
- Profile, budget, savings goal, overlap, and reminder preferences save atomically.
- Insight and reminder DTOs exclude ownership and transaction descriptions; audit events contain no financial details.

## Required before public deployment

| Finding | Exact task                                                                                                                                                                             | Priority | Dependencies                                | Effort | Verification method                                                                                                            | Blocking phase    |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------ | ----------------- |
| SEC-001 | Enforce the tested response-header policy in the production path. Roll CSP out in report-only mode first if required, then enforce without unsafe wildcards.                           | P2       | Final runtime endpoints and deployment edge | Medium | Automated production header assertions, CSP console/report review, and frame-embedding test                                    | Public deployment |
| SEC-010 | Define CI dependency policy: clean locked installs, advisory gate, reviewed package changes, lifecycle-script review, isolated credentials, provenance exceptions, and minimal egress. | P2       | CI/CD design                                | Medium | Clean CI run, package diff review, advisory result, signature result or approved exception, and least-privilege token evidence | Public deployment |
| SEC-011 | Enable protected branches, required reviews and checks, history-aware secret scanning, and credential-rotation procedures in the canonical repository.                                 | P2       | Git hosting administration                  | Medium | Repository-setting evidence and simulated secret-response exercise                                                             | Public deployment |
| SEC-006 | Verify production CDN, proxy, and browser cache behavior for private pages, session refresh, logout, export, and deletion.                                                             | P2       | Deployed staging environment                | Medium | Two-user cache-isolation test against production-equivalent infrastructure                                                     | Public deployment |
| SEC-007 | Configure security-event retention, restricted access, alerting, clock synchronization, and tamper detection.                                                                          | P2       | Monitoring and operations platform          | Medium | Alert simulation, access review, tamper test, and retention evidence                                                           | Public deployment |
| SEC-009 | Publish accurate privacy, retention, export, deletion, backup, and support-access behavior. Confirm that UI claims match implemented controls.                                         | P2       | Legal/product review and completed behavior | Medium | Copy review against executable deletion/export evidence                                                                        | Public deployment |

## Defense-in-depth improvements

| Finding | Exact task                                                                                                                                                                         | Priority | Dependencies                | Effort | Verification method                                                                           | Blocking phase                         |
| ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | --------------------------- | ------ | --------------------------------------------------------------------------------------------- | -------------------------------------- |
| SEC-002 | Evaluate React taint APIs to help detect accidental server-object serialization. Treat this as defense in depth, not authorization or DTO minimization.                            | P3       | Stable DAL and DTOs         | Small  | Deliberate serialization test fails in development and CI                                     | None after core DTO control exists     |
| SEC-005 | Place security-definer functions in a private schema, set an empty `search_path`, schema-qualify names, and minimize execution grants. Use `security_invoker` views where exposed. | P2       | Database functions or views | Small  | Database metadata assertions and unauthorized invocation tests                                | Before affected function or view ships |
| SEC-008 | Add property-based and coverage-guided parser fuzzing with a fixed resource budget and a corpus of previously failing fictional samples.                                           | P3       | Stable parser adapter       | Medium | Scheduled fuzz job completes within limits with no unhandled failure or retained raw artifact | Before broad file-format support       |
| SEC-010 | Generate and retain a software bill of materials and signed build provenance for releases.                                                                                         | P3       | CI/CD and release process   | Medium | Release artifact includes verifiable SBOM and provenance                                      | Future production maturity             |

## Future bank and email integration requirements

| Finding | Exact task                                                                                                                                                                            | Priority | Dependencies                                 | Effort | Verification method                                                                                              | Blocking phase                   |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- | -------------------------------------------- | ------ | ---------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| SEC-009 | Create a separate data-flow and retention review for every bank or email provider before selecting scopes or storing tokens.                                                          | P1       | Provider choice and product purpose          | Medium | Approved inventory lists every scope, field, recipient, retention period, deletion path, and support role        | Before provider proof of concept |
| SEC-004 | Use OAuth with exact redirect allowlists, state and PKCE where supported, CSRF protection, generic callback errors, and account-linking safeguards.                                   | P1       | Provider OAuth specification                 | Large  | Tests for state mismatch, redirect mismatch, callback replay, unlink, revoked consent, and wrong-account linking | Before provider proof of concept |
| SEC-005 | Isolate each provider adapter and token store from browser code and ordinary database queries. Use separate least-privilege roles and deny support access by default.                 | P1       | Secret store and adapter architecture        | Large  | Bundle and payload scans, role tests, and support-access tests                                                   | Before provider proof of concept |
| SEC-007 | Log only opaque connection IDs, event types, and safe provider error classes. Never log OAuth codes, access or refresh tokens, email content, transaction text, or provider payloads. | P1       | Provider event catalog                       | Small  | Captured-log scanning across success and failure callbacks                                                       | Before provider proof of concept |
| SEC-010 | Complete vendor, SDK, webhook, data-location, incident-response, and subprocessor review before adding a provider dependency.                                                         | P1       | Candidate provider contract and SDK          | Medium | Approved vendor assessment and locked dependency review                                                          | Before provider proof of concept |
| SEC-009 | Revoke provider access and delete stored tokens on disconnect and account deletion. Document any provider-side retention that SubTrack cannot control.                                | P1       | Provider revocation API and account deletion | Medium | End-to-end disconnect/deletion test and provider-side confirmation                                               | Before provider production use   |

## Tracking and closure

A finding closes only when its verification method passes and the evidence appears in the relevant security gate or release record. Documentation alone does not close an implementation finding. If implementation changes the trust boundary, update `THREAT-MODEL.md` before closing the item.
